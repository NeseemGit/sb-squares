/** Default commish notes shown when a pool has none set. Used in admin form default and pool detail display. */
export const DEFAULT_COMMISH_NOTES =
  "Numbers are hidden while squares are being claimed\nOnce the grid is full, numbers are randomly assigned\nNumbers are revealed at that time (or at kickoff)\nAll assignments are final";
